- [Flex layout table overflow on mobile](flex-min-width-table-overflow.md) — a wide `<table>` inside nested flex-1 containers (app shell layout) blows out the whole page width on mobile unless ancestors get `min-w-0`.
- [Orval zod codegen version bug](orval-zod-codegen-version.md) — Orval's zod client generation can silently emit a broken `catalog:`-style version string; pin `override.zod.version: 3` in the orval config.
- [Drizzle chained .where() TypeScript error](drizzle-chained-where.md) — a shared query-builder helper that already applies `.where()` breaks if a caller chains `.where()` again; expose a bare base query instead and let each caller supply the full condition.
- [Orval-generated list hooks: enabled option quirk](orval-hooks-enabled-quirk.md) — passing a partial `{ query: { enabled } }` to a generated `useList*` hook fails TS; omit the options arg and rely on the hook's built-in `enabled` default instead.
- [React Query + Orval mutations need manual invalidation](react-query-orval-invalidation.md) — Orval-generated create/update/delete hooks never invalidate the matching list query automatically; every mutation's onSuccess must call `queryClient.invalidateQueries` with the generated `getList*QueryKey`, or the UI silently shows stale data.
- [Clerk invite email matching must be case-insensitive](clerk-invite-email-case.md) — Clerk preserves user-typed casing on sign-up; matching an invited email to a new login by exact string equality intermittently fails multi-tenant invite linking unless both sides are lowercased.
- [Campaign job lease fencing](campaign-job-lease-fencing.md) — never bulk-requeue in-flight sends on pause; exact-lease settlement, abort signaling, and stale-lease recovery prevent duplicates and stuck jobs.
- [Org/workspace switch must hard-reload](org-switch-stale-cache.md) — clearing the query cache alone isn't enough when the server resolves tenant from a cookie; stale data can leak permission UI across orgs.
- [RBAC checks need cross-mutation symmetry](rbac-mutation-symmetry.md) — a rank rule enforced on "update" (e.g. only Owner can touch Owner) is easily forgotten on sibling mutations like "delete"; audit every mutation on a resource, not just one.
- [Serial sequence drift can hide queue data loss](serial-sequence-conflicts.md) — after copying explicit IDs, reset sequences; broad conflict-ignore can silently discard new queue rows on primary-key collisions.
- [Distributed send limits need one database clock](distributed-send-limit-clock.md) — reserve throughput and create leases in one transaction using the database clock, never a replica or tick-start clock.
- [Bounded queue scans need scope quotas](bounded-queue-scope-quotas.md) — a global candidate limit is not fair if one saturated scope can fill the whole prefix; quota candidates by limiting scope first.
- [Campaign benchmark metric semantics](campaign-benchmark-metric-semantics.md) — count claims at the queue boundary; never label provider-send attempts or liveness samples as throughput proof.
- [Clean-source benchmark bundles](clean-source-benchmark-bundles.md) — keep Node benchmark bundles package-local for module resolution, but Git-ignored so source-cleanliness checks remain valid.
- [Production-shaped campaign benchmarks](production-shaped-campaign-benchmarks.md) — throughput evidence must use frozen Plan→Execute job fields, an isolated DB, stopped API workers, and a clean source hash.
- [Composite TS project references redirect to stale dist](composite-tsc-stale-dist-redirect.md) — after merging lib/* schema changes, rebuild the lib (or run root typecheck) before trusting an artifact's own typecheck; stale dist declarations look like a broken merge.
- [drizzle-kit push fails non-interactively](drizzle-push-non-interactive.md) — no TTY in this shell; apply additive dev-schema changes via executeSql DDL derived from the schema diff instead.
- [Frozen campaign plan/execute split](campaign-frozen-plan-execution.md) — allocation freezes at plan time, jobs are created at execute time; mapping resolution must always prefer the frozen snapshot over live edits.
- [Campaign readiness needs two separate template steps](wabista-campaign-readiness-two-step.md) — a route's own template pick doesn't satisfy Plan; campaign-level template-mappings selection is a separate required step.
- [Org-scoped mutation pattern & error shape](wabista-org-scoped-mutations.md) — pages get organizationId via useListOrganizations+isActive; ApiError isn't exported, so read errors via duck-typed `.data`/`.status`.
- [Orval query-param collision on path-param operations](orval-query-param-collision.md) — adding a query param to an operation with existing path params can duplicate a `<OperationId>Params` export and break zod codegen's tsc build.
- [Shared campaign Plan/Execute lifecycle](wabista-shared-campaign-lifecycle.md) — Plan/Execute/readiness-dialog logic is a shared hook+component since multiple screens call the same backend contract; reuse it, don't reimplement.
- [Background workflow worker races tests](workflow-worker-races-tests.md) — a live campaign-runtime worker can intermittently race ANY integration test that creates "Queued"/leaseable rows under "Running", not just one known file; stop the workflow to rule it out before debugging.
- [Pre-signup team invitations design](wabista-pending-invitations.md) — invite-by-email creates a Pending row when no account exists yet; accepted automatically in attachOrgContext's first-login path, ahead of personal-org provisioning.
- [Cross-tenant isolation regression pattern](wabista-cross-tenant-isolation.md) — every org-scoped mutation must assert organizationId in the WRITE predicate itself, not just a preceding read; test with a real cross-org resource ID, not a fabricated one.
- [Postgres GROUP BY functional-dependency gotcha](postgres-group-by-joined-column.md) — selecting a joined table's column while grouping only by the primary table's PK compiles fine but fails at runtime; test aggregate endpoints against a real joined row.
- [Support/ops recipient visibility scope](wabista-support-recipient-visibility-scope.md) — "let support see what a plan will send" means within-org staff, not cross-tenant/platform access; reuse the shared resolution core for bulk views.
- [Orval binary/Blob request bodies get JSON.stringify'd](orval-blob-body-bug.md) — generated mutation functions for a `Blob`/binary request body silently corrupt uploads; hand-write the fetch call instead of using the generated hook.
- [Streaming CSV import row/blank-line semantics](csv-import-row-semantics.md) — rowsProcessed counts the header row too, and a genuinely blank trailing line yields a phantom empty data row; know both before trusting a row-count mismatch as a bug.
- [Scaling a list page: paginate + pg_trgm search](list-page-scale-pattern.md) — an unbounded list fetched whole and filtered client-side needs server-side limit/offset plus trigram GIN indexes for ILIKE substring search, not just a btree.
- [Regression coverage with no Playwright/Vitest installed](testing-subagent-as-committed-regression.md) — for a frontend-cache-only bug, document a fully-specified testing-subagent plan (with `[DB]` setup steps) in-repo as the reusable regression check instead of standing up a new test framework.
- [pgcrypto not enabled on this DB](pgcrypto-not-enabled.md) — gen_random_bytes()/gen_random_uuid() fail; use md5(random()::text || clock_timestamp()::text) for DB-generated random tokens/defaults instead.
- [Orval zod response schema and format:date](orval-date-format-coerce.md) — `format: date` in an OpenAPI response schema emits `zod.coerce.date()`, silently turning a "YYYY-MM-DD" string into a full ISO datetime when `.parse()`'d before `res.json()`.
- [Mock WhatsApp provider never auto-simulates delivery webhooks](mock-provider-no-delivery-simulation.md) — provider_messages stays at "sent" and provider_events stays empty in mock mode unless a webhook payload is actually posted; real/near-zero delivered counts in dev are expected, not a bug.
- [Throttled-route reactivation needs its own timestamp column](campaign-route-throttle-reactivation.md) — a shared `updatedAt`/`$onUpdate` column is unsafe for staleness checks; also: decoupling housekeeping from a bounded work loop needs deadlock-retry.
- [TS narrowing across closures](ts-narrowing-across-closures.md) — `foo.value` narrowed by an early-return guard re-widens to `T | undefined` inside a nested async callback (e.g. `db.transaction(async (tx) => ...)`); capture it to a local first.
- [Sustained per-number TPS test flaky under load](sustained-tps-test-flaky-under-load.md) — a wall-clock throughput test can fail with no code regression; re-run in isolation before blaming a change.
- [STOP suppression fencing window](stop-suppression-fencing-window.md) — advisory-lock fencing covers the DB reservation transaction but not the provider HTTP call after it; a STOP in that narrow post-commit window could still slip one message through. FIXED — see session-advisory-lock-across-external-call.md.
- [Session-scoped advisory lock across a DB decision + external call](session-advisory-lock-across-external-call.md) — a transaction-scoped lock releases before your own process resumes; hold it at session level and unlock explicitly once committed, without holding it through the network call itself.
- [Scale-safe search + count pagination](scale-safe-search-and-count-pagination.md) — replace a cross-table OR-ILIKE with a UNION of per-column trigram lookups; merge count(*)+select into one windowed query, falling back to a plain count only on empty pages.
- [Campaign metrics row is not auto-created](campaign-metrics-row-required.md) — plan/execute/claim/settle silently no-op their campaign_metrics UPDATEs (0 rows matched, no error) unless a metrics row was explicitly inserted first; test fixtures that skip real CSV import must insert one manually.
- [Rate-limit fairness test intermittently flaky](campaign-rate-limit-fairness-flaky.md) — "multiple queues share route and phone throughput atomically" fails ~1/3 runs in total isolation with unrelated code; treat as known flaky timing behavior, not a regression signal, unless it fails consistently.
- [WhatsApp per-number TPS approved cap](wabista-phone-tps-approved-cap.md) — Meta's real signal is `throughput.level` (STANDARD=80mps/HIGH=1000mps), not a literal TPS field; upsert must rewrite it on every re-sync or the cap can never change.
- [Benchmark DB shares compute with dev DB](wabista-benchmark-shared-compute-contention.md) — a dedicated benchmark database on the same Postgres server still contends with the live api-server workflow; stop that workflow during benchmark runs or throughput drops 3-6x and drain-timeout assertions fail.
- [Campaign claim query scaled with queue depth](campaign-claim-query-queue-depth-bottleneck.md) — per-route top-N ranking via window function over the whole queue, not LATERAL+index, made claim() cost scale with backlog size; also covers sizing benchmark drain timeouts from observed rate.
- [Real OS-process crash recovery testing pattern](os-level-crash-recovery-testing.md) — spawn the runtime as its own process, build harness inside the package (not /tmp) for ESM external resolution, use delay-once + audit-log provider hooks to catch a real in-flight send.
- [Clerk + Playwright programmatic E2E pattern](clerk-playwright-e2e-pattern.md) — sign in via `@clerk/testing` without the UI, seed via drizzle from a frontend package, always use `$REPLIT_DEV_DOMAIN` (never localhost:port) as baseURL when services share a path-based proxy.
- [esbuild must stay external in nested bundling](esbuild-package-must-stay-external.md) — a test file that itself imports `esbuild` breaks with "__filename is not defined" if the outer bundler inlines esbuild instead of externalizing it.
- [High-TPS route lock convoys](high-tps-route-lock-convoys.md) — shared config locks permit concurrent safe claims; per-window route-status writes serialize lanes, and the next ceiling requires batching counters/claims.
- [Rocket route matrix keeps one route per number](rocket-one-route-per-number.md) — rotate selected templates across numbers; duplicating one number across template routes multiplies its route-level TPS budget.
- [Batched rate windows must guard inserts](batched-rate-window-insert-safety.md) — conflict predicates protect updates only; reject an oversized token batch before inserting a new limiter window.
- [Hot-path ledgers must avoid parent FKs](hot-ledger-parent-fk-locks.md) — PostgreSQL FK checks take parent KEY SHARE locks, silently coupling append-only deltas back to campaign pause/settlement locks.
- [Per-phone dispatch workers need real-boundary acknowledgements](per-phone-dispatch-worker-acks.md) — pace in an isolated worker, but advance cadence only when the main thread actually starts the provider call.
- [Rocket reservoir settlement batching](rocket-reservoir-settlement-batching.md) — DB-free transport still starves if per-message settlement pins route capacity; settle by campaign in set-based batches.
- [Multi-phone main-thread handoff ceiling](multiphone-main-thread-handoff-ceiling.md) — phase staggering fixes two-lane wakeup collisions; four-plus lanes need sharded/batched main-thread handoffs, not more phase tuning.
- [Broker crash recovery must isolate uncertainty](broker-crash-recovery-isolation.md) — reclaim only superseded fences, preserve ambiguous publishes, fence exact leases, and never let one unknown provider intent fail its whole preparation batch.
- [Four-phone benchmark supply ceiling](four-phone-benchmark-supply-ceiling.md) — current host/profile under-feeds 1,000 TPS per phone; larger refill batches stall startup instead of improving throughput.
- [Provider settlement backpressure fencing](provider-settlement-backpressure.md) — reserve settlement capacity before provider I/O; full capacity requeues without consuming attempts, while ACK debt counts against lane capacity.
- [Benchmark preflight versus reproduction](benchmark-preflight-vs-reproduction.md) — count only runs that enter the workload as reproductions; report database/name/config preflight rejections separately.
- [Benchmark runtime lifecycle](benchmark-runtime-lifecycle.md) — every throwaway CampaignRuntime must be closed or its workers can overwrite global metrics and keep the benchmark process alive.
- [Persisted reservoir fix still has warm gate](persisted-reservoir-fix-warm-gate.md) — the archived fixed reservoir still waits for low-water before consuming; a full four-phone certification run can fail before emitting results.
- [Post-publish consume must drain](post-publish-consume-drain.md) — removing the warm gate is incomplete unless the same post-publish path immediately drains consumed envelopes to transport.
- [Benchmark recovery gate isolation](benchmark-recovery-gate-isolation.md) — a recovery assertion for one abandoned lease requires exactly one recovery claimant and no competing runtime claim.
- [Reservoir broker depth accounting](reservoir-broker-depth-accounting.md) — broker depth already includes queued/in-flight published work; add only reserved claims when calculating supply watermarks.
- [Reservoir pacing request contract](reservoir-pacing-request-contract.md) — refill claim batches must not exceed the pacing coordinator's bounded reservation size or refills fail before PostgreSQL.
